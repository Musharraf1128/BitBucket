# Environment Variables Template
# Copy this file to .env and fill in your values
# WARNING: Never commit .env file to version control!

# JWT Configuration
# IMPORTANT: Generate a secure random secret for production
# You can generate one using: openssl rand -base64 64
JWT_SECRET=YnpridNmRsM/BblbgxB+pUbn5eGik87nypBT73eK8fTlMifJKP53sTLHIbhcCw2TxKofdwCe5rclYqCVPJyHVQ==

# Database Configuration (only if not using Docker)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres

# Redis Configuration (only if not using Docker)
REDIS_HOST=localhost
REDIS_PASSWORD=

# Cache Type (set to 'redis' to enable caching, 'none' to disable)
CACHE_TYPE=redis

# File Upload Directory
FILE_UPLOAD_DIR=/app/uploads

# Email Configuration (SMTP)
# For Gmail: Use App Password, not your regular password
# Generate at: https://myaccount.google.com/apppasswords
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
